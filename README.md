loadbalancer.c is a loadbalancer that can distribute load between N servers

To run, type make, then ./loadbalancer with args portnumber, logging(optional), number of threads(optional), serverport1, serverport2, ...

